export interface PostFrontmatter {
	title: string;
	date: string;
	description: string;
	slug: string;
	published: boolean;
}

export interface PostMeta {
	title: string;
	date: string;
	description: string;
	slug: string;
}
